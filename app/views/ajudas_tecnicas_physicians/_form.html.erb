<%= form_for(@physician) do |f| %>
	<%= render 'shared/error_messages', obj: @physician %>

	<%= f.label :nome_medico %>
	<%= f.text_field :nome_medico, class: 'form-control' %>

	<%= f.submit "Submeter", class: 'btn btn-success' %>
<% end %>