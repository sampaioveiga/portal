<ul class="nav nav-pills nav-stacked">
	<li role="presentation" class=<%= 'active' if current_page?(patients_path) %>>
		<%= link_to patients_path() do %>
			<span class="glyphicon glyphicon-user" aria-hidden="true"></span> Doentes
		<% end %>
	</li>
	
	<!-- if user is supervisor -->
	<%- if controller_name == 'patients' && action_name == 'show' # && policy(:user).edit? %>
		<li role="presentation">
			<%= link_to edit_patient_path(@patient), class: 'text-success' do %>
				<span class="glyphicon glyphicon-pencil" aria-hidden="true"></span> Editar doente
			<% end %>
		</li>
		<!--<li role="presentation">
			<%= link_to patients_path(@patient), method: :delete, data: { confirm: 'Tem a certeza?' }, class: 'bg-danger' do %>
				<span class="glyphicon glyphicon-erase" aria-hidden="true"></span> Eliminar
			<% end %>
		</li>-->
	<% end %>

	<!-- UCI menu -->

	<!-- if Cateres -->
	<%- if controller_name == 'patients' && action_name == 'show' # && policy(:user).edit? %>
		<li role="presentation">
			<a href="#" data-toggle="modal" data-target="#newCatheterModal">
				<span class="glyphicon glyphicon-plus" aria-hidden="true"></span> Registar cateter
			</a>
		</li>
	<% end %>
	<!-- Cateres -->

	<!-- if UCI supervisor -->
	<!--	<li role="presentation" class=<%= 'active' if current_page?(catheters_path) %>>
			<%= link_to catheters_path() do %>
				<span class="glyphicon glyphicon-pencil" aria-hidden="true"></span> Tipos de cateter
			<% end %>
		</li>-->
	<!-- end if -->

	<!-- end UCI menu -->

	<!-- Cancel action if edit or new  -->
	<%- if action_name == 'edit' || action_name == 'new' %>
		<li role="presentation">
			<%= link_to :back, class: 'text-muted' do %>
				<span class="glyphicon glyphicon-arrow-left" aria-hidden="true"></span> Cancelar
			<% end %>
		</li>
	<% end %>
</ul>