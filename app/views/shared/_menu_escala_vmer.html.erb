<ul class="nav nav-pills nav-stacked">
	<li role="presentation" class=<%= 'active' if current_page?(vmer_escala_vmer_schedules_url()) %>>
		<%= link_to vmer_escala_vmer_schedules_url() do %>
			<span class="glyphicon glyphicon-calendar" aria-hidden="true"></span> Disponibilidades
		<% end %>
	</li>

	<li role="presentation" class=<%= 'active' if current_page?(new_vmer_escala_vmer_schedule_url) %>>
		<%= link_to new_vmer_escala_vmer_schedule_url(), class: 'text-success' do %>
			<span class="glyphicon glyphicon-plus" aria-hidden="true"></span> 
			Adicionar disponibilidade
		<% end %>
	</li>

	<li role="presentation" class=<%= 'active' if current_page?(vmer_escala_vmer_maps_url) %>>
		<%= link_to vmer_escala_vmer_maps_url() do %>
			<span class="glyphicon glyphicon-print" aria-hidden="true"></span> 
			Ver escala
		<% end %>
	</li>
	
	<!-- if Users -->
	<%- if controller_name == 'users' && action_name == 'show' && policy(:user).edit? %>
		<li role="presentation">
			<%= link_to edit_contacts_user_path(@user), class: 'text-success' do %>
				<span class="glyphicon glyphicon-pencil" aria-hidden="true"></span> Editar perfil
			<% end %>
		</li>
	<% end %>

	<!-- if Departments -->
	<%- if controller_name == 'ulsne_departments' && action_name == 'index' && policy(:ulsne_department).new? %>
		<ul class="nav nav-pills nav-stacked">
			<li role="presentation">
				<%= link_to new_contacts_ulsne_department_path(), class: 'text-success' do %>
					<span class="glyphicon glyphicon-plus" aria-hidden="true"></span> Novo serviço
				<% end %>
			</li>
		</ul>
	<% end %>
	<%- if controller_name == 'ulsne_departments' && action_name == 'show' && policy(:user).edit? %>
		<ul class="nav nav-pills nav-stacked">
			<li role="presentation">
				<%= link_to edit_contacts_ulsne_department_path(@ulsne_department), class: 'text-success' do %>
					<span class="glyphicon glyphicon-pencil" aria-hidden="true"></span> Editar dados
				<% end %>
			</li>
		</ul>
	<% end %>

	<!-- if Sites  -->
	<%- if controller_name == 'ulsne_sites' && action_name == 'index' && policy(:ulsne_site).new? %>
		<ul class="nav nav-pills nav-stacked">
			<li role="presentation">
				<%= link_to new_contacts_ulsne_site_path(), class: 'text-success' do %>
					<span class="glyphicon glyphicon-plus" aria-hidden="true"></span> Nova unidade
				<% end %>
			</li>
		</ul>
	<% end %>

	<!-- if Edits  -->
	<%- if action_name == 'edit' || action_name == 'new' %>
		<li role="presentation">
			<%= link_to :back, class: 'text-muted' do %>
				<span class="glyphicon glyphicon-arrow-left" aria-hidden="true"></span> Cancelar
			<% end %>
		</li>
	<% end %>
</ul>

<% if current_user.administrator || current_user.escala_vmer_user.nivel_acesso == 2 %>
	<div class="panel panel-default">
		<div class="panel-heading">Escala VMER</div>
		<ul class="list-group">
			<li class="list-group-item">
				<span class="glyphicon glyphicon-cog" aria-hidden="true"></span> <a href="<%= vmer_escala_vmer_groups_path() %>">Grupos</a>
				<br />
				
				<span class="glyphicon glyphicon-user" aria-hidden="true"></span> <a href="<%= vmer_escala_vmer_users_path() %>">Utilizadores</a>
			</li>
		</ul>
	</div>
<% end %>
<%- if controller_name == 'escala_vmer_schedules' %>
	<% unless current_user.escala_vmer_user.nil? %>
		<%= render 'group_list' %>
	<% end %>
<% end %>