<aside class="col-md-2">
	<%= render 'shared/transp_menu' %>
</aside>
<div class="col-md-9">
	<table class="table">
		<thead>
			<tr>
				<th>Destino</th>
			</tr>
		</thead>
		<tbody>
			<% @destinations.each do |destination| %>
				<tr>
					<td><%= link_to destination.destino, edit_transp_destination_path(destination) %></td>
				</tr>
			<% end %>
		</tbody>
	</table>
	<h2>Novo destino</h2>
	<%= render 'form' %>
</div>