<%= form_for @catheter do |f| %>
	<%= render 'shared/error_messages', obj: @catheter %>

	<div class="col-md-9">
		<%= f.text_field :nome_cateter, class: 'form-control' %>
	</div>
	<div class="col-md-3">
		<%= f.submit 'Submeter', class: 'btn btn-primary' %>
	</div>
<% end %>